{% extends '@MoonManager/base.html.twig' %}

{% block title %}Hello UserController!
{% endblock %}

{% block body %}

	<div class="mws-user-index" {{ svelte_component('MwsUserList', { 'users': [ 'hello', 'world'], } ) }}>

		{% if app.user %}
			<a href="{{ path('mws_user_logout') }}">
				<u>Logout from
					{{ app.user.username }}</u>
			</a>
		{% endif %}

		<div class="example-wrapper">
			<h1>Hello
				{{ block('title') }}! ✅</h1>
		</div>

	</div>

{% endblock %}
