{# 🌖🌖 Copyright Monwoo 2023 🌖🌖, build by Miguel Monwoo (service@monwoo.com) #}
{% extends '@MoonManager/base.html.twig' %}

{% block title %}Hello {{ app.user.username }}
{% endblock %}

{% block body %}
	<div class="mws-user-show" {{ svelte_component('MwsUserShow', {
		'copyright': 'mws.copyright'|trans({}, 'mws-moon-manager'),
		'locale': app.locale,
		'user': user|serialize('json')|e('html_attr'),
		'viewTemplate' : viewTemplate,
	} ) }}>
		{% if app.user %}
			<a href="{{ path('mws_user_logout') }}">
				<u>Logout from
					{{ app.user.username }}</u>
			</a>
		{% endif %}

		<div class="example-wrapper">
			<h1>{{ block('title') }}! ✅</h1>
		</div>
	</div>
{% endblock %}
